import { HurryTimeDirective } from './hurry-time.directive';

describe('HurryTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new HurryTimeDirective();
    expect(directive).toBeTruthy();
  });
});
